import React from 'react';
import { Box, Typography } from '@mui/material';

const Rodape = () => {
  return (
    <Box
      sx={{
        position: 'fixed', 
        bottom: 0,
        width: '100%',
        backgroundColor: '#333',
        color: '#fff',
        padding: '10px 0',
        textAlign: 'center',
        fontSize: '14px',
        zIndex: 1000, 
      }}
    >
  
    </Box>
  );
};

export default Rodape;
